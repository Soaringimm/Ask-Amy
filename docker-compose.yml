version: '3.8'

services:
  ask-amy:
    build:
      context: .
      args:
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
        VITE_CAL_USERNAME: ${VITE_CAL_USERNAME}
        VITE_CAL_API_KEY: ${VITE_CAL_API_KEY}
    container_name: ask-amy
    environment:
      - ASK_AMY_API_KEY=${ASK_AMY_API_KEY}
    ports:
      - "8507:80"
    networks:
      - immicore-network
    restart: always

networks:
  immicore-network:
    external: true
